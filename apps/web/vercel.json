{
  "installCommand": "npm install -g pnpm@9.15.0 && pnpm install --dir ../.. --no-frozen-lockfile",
  "buildCommand": "pnpm --dir ../.. --filter web build",
  "outputDirectory": "dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
